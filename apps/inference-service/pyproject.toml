[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "careai-inference-service"
version = "0.1.0"
description = "Inference service for careai-platform."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

