[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "train-claims-risk"
version = "0.1.0"
description = "Synthetic claims-risk ML training pipeline for careai-platform."
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

