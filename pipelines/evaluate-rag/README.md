# evaluate-rag

Placeholder for the RAG evaluation pipeline. The current LLMOps ingestion foundation lives in `pipelines/ingest-rag`, which loads synthetic documents, chunks text, embeds content, and indexes into Azure AI Search or local JSON fallback.

Future evaluation work should add synthetic retrieval test cases, prompt evaluation, safety checks, groundedness checks, role-filter tests, and audit-ready evaluation reports.
